server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # React Router / refresh support (safe even if you mostly use one page)
    location / {
        try_files $uri $uri/ /index.html;
    }
}